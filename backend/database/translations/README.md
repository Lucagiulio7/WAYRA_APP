# Content translations

Add one UTF-8 JSON file per language, named with its language code, for example `es.json`. The backend discovers it automatically. Keys identify records by their stable canonical Italian name.

See `mobile/i18n/README.md` for the complete schema and client registration steps. Legacy French content embedded in `static_city_content.json` is loaded through this same contract.
